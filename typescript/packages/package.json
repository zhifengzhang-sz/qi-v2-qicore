{
  "name": "@qi/packages",
  "version": "2.0.0",
  "private": true,
  "workspaces": ["base", "core"],
  "scripts": {
    "build:base": "cd base && bun run build",
    "build:core": "cd core && bun run build",
    "build": "bun run build:base && bun run build:core",
    "test:base": "cd base && bun run test",
    "test:core": "cd core && bun run test",
    "test": "bun run test:base && bun run test:core",
    "clean": "cd base && bun run clean && cd ../core && bun run clean"
  }
}
